# Results

Generated metrics belong in this directory as compact CSV or JSON files. Large checkpoints, reconstructed images, prepared datasets, and per-run output folders are ignored by Git.

Use scripts/reproduce_tables.py to aggregate all matching run files under outputs/.
